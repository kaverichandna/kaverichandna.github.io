I"¸<p>This example contains displaying GPS from uBlox Neo-6M module on the serial monitor on a ESP-32 based LilyGO T-Beam <code class="language-plaintext highlighter-rouge">T22_V1.1, 20191212</code>. Pins <code class="language-plaintext highlighter-rouge">IO12</code> is used as <code class="language-plaintext highlighter-rouge">TX</code> and pin <code class="language-plaintext highlighter-rouge">IO34</code> is used as <code class="language-plaintext highlighter-rouge">RX</code> as shown in the <a href="https://github.com/Xinyuan-LilyGO/LilyGO-T-Beam/blob/master/schematic/LilyGo_TBeam_V1.1.pdf">schematic for V1.1</a>.</p>

<ol>
  <li>Flash the firmware with the <code class="language-plaintext highlighter-rouge">make</code> command.</li>
  <li>Connect the serial monitor to view the raw NMEA sentences.</li>
  <li>Leave it in the open outdoors for a while to get a GPS fix.</li>
  <li>Ensure RED LED is blinking to show the time pulse.</li>
</ol>
:ET